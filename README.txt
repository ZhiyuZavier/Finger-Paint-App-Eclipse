1. XML shapes specified as background shape of colour buttons.
2. Images specified as background image of shape buttons.
3. Implement a save button – allow a user to save the current screen as a bitmap.
4. Implement an email button – allow a user to email the contents of the screen.
5. Have the system respond to different screen sizes - using RelativeLayout and LinearLayout
6. Implement multi touch paint – allow the user to draw multiple lines at the same time with different fingers, using MotionEvent properties to keep track of individual touches across multiple touch events.
7. Have the application respond to the pressure of a MotionEvent - A large circle indicates the current position of a touch, while smaller trailing circles represent previous positions for that touch. The size of the large circle is scaled depending on the pressure of the user's touch.